export interface AcademyThemeDto {
  id: number;
  primaryColor: string;   // Color primario, por defecto "#000000"
  secondaryColor: string; // Color secundario, por defecto "#FFFFFF"
  accentColor: string;    // Color de acento, por defecto "#FF0000"
}
